# Ruff formatting/linting defaults to keep style consistent across the API codebase.
line-length = 120
target-version = "py311"

[lint]
select = ["E", "F", "I", "W"]

[format]
quote-style = "double"
indent-style = "space"
